set (PROJECTNAME ${CMAKE_SCRIPTS_NAME})

project( ${PROJECTNAME} )

include(${PROJECTNAME}.list)


add_library(${PROJECTNAME} STATIC ${CMAKE_CONFIG_FILES} ${CMAKE_ROOT_FILES} ${CMAKE_SCRIPT_FILES})

set_target_properties(${PROJECTNAME} PROPERTIES FOLDER ${CORE_FOLDER_NAME})
